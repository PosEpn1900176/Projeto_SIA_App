import React from 'react';
import InnocentiPresentation from '../Presentation';

const InnocentiContainer = props => {
    return (
        <InnocentiPresentation />
    );
};

export default InnocentiContainer;